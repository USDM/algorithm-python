class Point:
  def __init__(self, x,y):
    self.x=x
    self.y=y

class BresenhamCircle:

  def __init__(self, center, radius):
    self.center = center
    self.radius = radius
    pass

  def draw_circle(self):
    x=0
    y=radius
    while x != y:

  def _plot_point(self, point):



center = 
radius = 5
bresenhamCircle = BresenhamCircle()
bresenhamCircle.draw_circle(center, radius)